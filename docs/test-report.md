# 食衡测试与发布验证报告

## 1. 基线

- 测试日期：2026-09-12
- 源码分支：`feat/shihe-mvp`
- RC1 源码提交：`6d70077`
- 包名：`com.openvela.contest2026.team474.shihe`
- 版本：`1.0.0`（`versionCode: 1`）
- 历史 RC1 文件：`com.openvela.contest2026.team474.shihe.release.1.0.0.rpk`
- RC1 大小：90,125 bytes
- RC1 SHA-256：`3e26977cf4d9c8696d5823b07ace1c9699de89264ec8ad86070f2c7c21b5728e`

本报告只记录已经实际完成的验证。上述 RC1 早于最终定版源码，只保留为历史发布基线，不是最终提交包。用户私下配置 `tp-` Key 后的 VelaClaw 成功回复尚未作为通过项；它不是 P0 发布门禁。

## 2. 自动化与构建

在 Ubuntu VM 和 Windows AIoT 工具链执行：

```bash
cd quickapp/shihe
npm test
npm run build
```

结果：

- 食品文本解析、未知项保留、餐食确认与回算测试通过。
- MET 运动估算、今日能量、7 天趋势、30 天保留和餐时状态测试通过。
- 健康样本归一化、无响应 watchdog、高压力本地建议测试通过。
- VelaClaw 脱敏上下文、基础设施错误识别、空回复和 10 秒超时回退测试通过。
- goldfish 缺少 `system.router` 时的入口降级测试通过。
- debug RPK 构建通过；构建审计确认 index、onboarding、home、meal、confirm、exercise、history 七个页面均进入包内。

### 2026-09-12 可用性修订回归

- 首页主信息已改为“今日已摄入”，同时独立展示“每日饮食目标”“手动补录运动”和“估算净摄入”。运动记录只改变手动补录消耗与估算净摄入，不改变饮食目标进度。
- 固定样例 `摄入 1200 / 手动补录运动 110 / 饮食目标 2000` 的自动断言结果为：饮食目标差值 800、估算净摄入 1090。
- 记餐页首次打开默认显示“一句话记餐”；在 466×466 比赛健康镜像中使用示例“米饭200克、鸡胸肉150g”，识别为米饭 232 kcal、鸡胸肉 248 kcal，并成功进入确认页。
- 切换到“快捷选择”后，最近食品列表恢复纵向布局；最近食品、收藏套餐和 60 项分类目录继续复用原有确认流程。
- 首次设置页已明确“每日饮食目标”指一天计划从食物中摄入的热量，不是运动消耗目标；运动页明确当前版本不能读取系统步数或运动记录，数据来自手动补录与 MET 估算。
- 本轮新增界面文案与默认入口静态测试；连同食品解析、能量、健康、VelaClaw、入口降级等共 8 个 Node 测试文件全部通过，七页面打包审计和 debug RPK 构建通过。

### 2026-09-12 最终定版自动回归

- 每日饮食参考目标编辑范围为 1200–4000 kcal、步长 100 kcal；低于 1500 kcal 显示专业指导提示。历史 V1 的 800–5000 kcal 读取仍兼容，编辑时只定位到最近边界，保存前不改原值。
- “台湾卤肉饭、番茄炒蛋盖饭、鱼香肉丝盖饭、牛肉面”四个套餐模板的组成、默认份量、半份/一份/两份缩放和热量断言通过；“不要蛋、少饭、加肉”等修饰语会进入待确认，不会静默忽略。
- 快捷选择的 60 项总数、分类计数、重复合并，以及已选项的增加、减少、移除和清空静态审计通过。
- 今日饮食/记录/多样三环与 7 天有记录天数、仅有记录日平均摄入、跨天食品 ID 去重的纯计算测试通过；补录运动不会改变饮食环。
- VM 再次执行全部 8 个 Node 测试文件、七页面 bundle 审计和 debug RPK 构建，结果全部通过；无新增外部 API、权限、路由或持久化版本。
- 本段自动测试与构建通过后，继续完成了下述最终定版 debug 运行回归；20 分钟稳定性仍按后续章节执行，未提前记为通过。

### 2026-09-14 最终定版 debug 运行回归

- 在 466×466 比赛健康镜像冷安装新版 debug RPK。该镜像的 `pm install` 为异步操作，实测约 30 秒后安装目录替换完成；等待安装结束再执行 `am start`，应用正常进入首次设置。
- 首次设置页显示每日饮食参考目标默认 2000 kcal、1200–4000 kcal 产品范围与非运动消耗说明；主动确认体重和免责声明后保存成功。
- 首页在空状态和已记录状态均保持“标题 → 餐时状态 → 今日已摄入与三环 → 记录一餐”层级；主按钮在 466×466 首屏完整可见。保存台湾卤肉饭后，页面显示 676 kcal、饮食环 34%、正餐 1/3、种类 4/12。
- “一句话记餐”默认页使用比赛镜像稳定支持的文本 picker 选择模拟器转写文本，避免该镜像文本输入控件的叠层问题；选择台湾卤肉饭后正确展开四项组成、约 676 kcal 和份量假设，并进入确认页。
- 确认页的增减与删除按钮可见；快捷选择显示“全部（60）/ 当前显示 60 / 共 60 项”，加入米饭后出现减量、加量、移除和清空，实际点击移除后恢复 0 项。分类 picker 已改用官方事件字段 `newValue`。
- 首页下部实际显示 `service.health` 官方 Mock 心率 90、血氧 99、压力 30；最近 7 天页显示 1/7 有记录天数、676 记录日均 kcal、4/25 食品种类。
- 480×480 方屏与 336×480 窄屏完成冷启动/首次设置布局烟测，标题、两张设置卡和窄屏餐段无横向溢出。七页全量三尺寸回归仍与 20 分钟稳定性一起保留为发布冻结门禁。

Windows 使用本地且被 Git 忽略的签名材料执行 `npm run release` 成功，生成上述历史 RC1。签名私钥未复制到 VM、比赛仓或证据目录。最终定版 production RPK 将在用户界面审核通过后重新生成。

## 3. AIoT 比赛健康模拟器

验证环境：

- AIoT Core / Emulator 1.7.22
- 镜像标签：`vela-miwear-watch-5.0（开发者大赛）`
- image type：`vela-miwear-watch-5.0-beta`
- 实例：`shihe-health-watch`
- 466 × 466 圆屏，density 320

历史 RC1 production RPK 通过 `pm install` 安装，并通过 `am start com.openvela.contest2026.team474.shihe` 启动。首次设置、首页、记餐、确认、补录运动和历史页面均能打开；重启后设置与业务记录保留。本结论不替代最终定版 debug 截图审核或最终 release 复测。

`service.health` 实际订阅得到官方 Mock 数据。历次观察包括 HEART_RATE/SPO2/STRESS 为 81/99/41、89/98/42、108/99/12、120/97/37，以及 RC1 验收时的 97/98/34。数值仅证明模拟器 Mock 链路，不代表真实健康测量。压力较高时本地建议会切换为一分钟呼吸提醒；离开首页后退订，返回首页重新建立一组订阅。

在普通 `vela-watch-5.0` 镜像中，运行日志确认 `service.health` 未注册。食衡稳定显示“当前设备暂不支持”，记餐、补录运动、历史和设置仍可使用。

### 20 分钟稳定性

状态：**待重新执行，不计为通过**。首轮人工核对确认四个业务页面可达，但自动循环后半段的返回手势离开了快应用并进入系统表盘卡片。原脚本只检查截图文件大小，没有验证页面语义，因此 1,200 秒和 23 个循环不能证明食衡四页持续可达。该次运行仅保留为“自动化脚本需要页面识别”的问题证据，不纳入稳定性结论。

正式复测必须在每轮先显式启动食衡，并验证截图中出现对应页面标题；通过标准仍为连续 20 分钟无应用崩溃、无黑屏，首页、历史、补录运动和记餐持续可达。复测完成前，本报告不得宣称 20 分钟稳定性已通过。

## 4. openvela goldfish

构建目标：`vendor/openvela/boards/vela/configs/goldfish-arm64-v8a-ap/`。

已验证配置包含：

```text
CONFIG_QUICKAPP=y
CONFIG_QUICKAPP_VAPP=y
CONFIG_FEATURE_SYSTEM_VELACLAW=y
CONFIG_EXAMPLES_AI_AGENT_VELA=y
CONFIG_MQ_MAXMSGSIZE=4096
```

已完成 debug RPK 启动和 VelaClaw 无 Key 降级验证：调用到达 `system.velaclaw` 和 `ai_agent`，后端返回不可用时，食衡识别基础设施错误并显示“AI 暂不可用，已保留本地建议”。

RC1 production RPK 也已完成实际验证：RPK 解包为 14 个文件并推送到 `/data/app/com.openvela.contest2026.team474.shihe`，串口执行 `vapp hap://app/com.openvela.contest2026.team474.shihe` 后，日志确认：

- 从生产包目录初始化，识别包名 `com.openvela.contest2026.team474.shihe` 和版本 `1.0.0`；
- `system.storage` 与 `system.velaclaw` 成功加载；
- `pages/index` 完成 build、ready 和 show；
- 应用持续运行且未自行退出，随后由测试人员正常中止并返回 `goldfish-armv8a-ap>` 提示符。

当前开源 goldfish 虽在构建配置中启用了 router，运行时仍未注册 `system.router`；根 URI 因此展示 index 验证页，完整七页流程由 AIoT 比赛模拟器承担。goldfish 输出中的 LVGL `CRIT [User]` 是该端口的 framebuffer 初始化日志级别，后续显示初始化和 QuickApp 启动均继续成功，不能单独按崩溃解读。

## 5. 数据与降级验证

- 空状态：首次启动进入设置，不使用虚假默认体重。
- 损坏存储：提示用户并允许明确重建，不静默覆盖。
- 未知食品：展示待处理文本，必须替换或明确忽略后才能确认。
- 断网或 AI 不支持：本地食品库、记餐、运动、历史和规则建议继续工作。
- 健康能力缺失、暂时失败、无有效样本：分别显示对应状态，不阻塞主流程。
- 运动来源：界面统一标注“手动补录 · MET 估算”，不宣称同步系统步数或运动记录。
- 修改和删除：餐食与运动变更后，食物摄入、手动补录消耗和估算净摄入立即重算；只有食物摄入会改变每日饮食目标进度。

## 6. 安全与隐私

- Git 跟踪文件和 RPK 文本均扫描高置信度 `tp-`、`sk-` 和私钥头模式。
- `sign/`、`node_modules/`、`build/` 和 `dist/` 被忽略。
- VelaClaw 只发送当天汇总和布尔状态，不发送原始健康样本；调用前要求用户确认。
- `tp-` Key 仅由用户在 goldfish `ai_agent` 中私下配置，不进入应用、仓库、AI 日志、截图或视频。
- 官方 `validate-log.py` 用于校验 `logs/AuroraZW/`；不得手工改写 JSONL。

## 7. 尚未关闭的非 P0 项

- VelaClaw 有 Key 成功回复需要用户私下完成一次最终验证；失败时保留本地建议，不阻塞发布。
- `system.alarm` 未纳入发布基线；餐时胶囊在应用打开或恢复时判断。
- 初赛不依赖真机、系统运动数据同步、手机伴侣或云服务。
- 480×480 方屏和 336×480 窄屏已完成首次设置布局烟测；七页全量三尺寸检查和连续 20 分钟稳定性复测仍须在发布冻结前完成，本轮 466×466 关键流程截图不替代这两项。
